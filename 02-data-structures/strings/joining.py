def test_join():
  text = ['I', 'love','programming']
  # join elements of text with space
  print(' '.join(text))
  # join elements of text with newline
  print('\n'.join(text))
  # join with dictionaries
  dict = {'a': 1, 'b': 2}
  print('->'.join(dict))
